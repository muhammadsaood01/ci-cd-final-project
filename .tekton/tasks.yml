apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup
spec:
  steps:
    - name: cleanup-step
      image: busybox
      script: |
        echo "Cleaning up..."

---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: nose
spec:
  steps:
    - name: run-tests
      image: python:3.9
      script: |
        nosetests -v --with-spec --spec-color --with-coverage --cover-package=app
